[features]
seeds = false
skip-lint = false

[programs.localnet]
risk_oracle = "REPLACE_WITH_DEPLOYED_PROGRAM_ID"

[provider]
cluster = "Devnet"
wallet = "../updater/keypair.json"

[scripts]
test = "yarn ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
